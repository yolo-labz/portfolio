{
	"name": "@portfolio/exec-job-board",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "cd site && pnpm dev",
		"build": "cd site && pnpm build",
		"collect": "python -m collector.main",
		"lint": "echo 'Lint handled per-service'",
		"typecheck": "cd site && pnpm typecheck"
	}
}
