# R2 — Portfolio retirement and dependency reconciliation

**21/09/2026, 09:46 BRT · desktop · implementation verified locally; NOT merged or deployed.**

## Scope and provenance

- Repo/worktree: `yolo-labz/portfolio`, `portfolio-138-retirement-swarm`.
- Branch: `138-retirement-swarm`; base `bf3ca9b3f3fdc890387c5c84601a96f47bc14376`.
- Generator: **OpenAI, GPT-6 Astra**. Full GLM queue/HTTP503 failure recovery was explicitly authorized. Prior GLM phase was read-only; its session was not resumed. No reviewer was spawned.
- Head: the commit carrying this report on the branch; resolve with `git rev-parse HEAD`. Final implementation/report commit and PR URL are recorded below after publishing.
- Only real-estate is retired. AI-document processor, exec-job board and serverless source, their manifests, and the existing visual spec are unchanged (`git diff HEAD -- <retained paths>` before the implementation commit was empty).
- No workflow, secret, account, production service, branch protection, or visibility changes. No social/media-service upload. Existing PR #135 was inspected read-only, not copied or closed.

## Hypothesis and observed root cause

**Hypothesis:** the obsolete catalogue entry, localized promises and independent workspace keep publishing/building a retired demonstration.

The recovered read-only HTTP evidence returned nginx welcome HTML for the advertised demonstration and HTTP404 for its API, despite the root returning 200. See `evidence/swarm-2026-09-21/prior-http.log`; this is prior-phase evidence, not a new implementation-seat measurement.

`apps/web/src/data/projects.ts` feeds the home shelf, `/[locale]/projects/[slug]` static params and sitemap. Removing its one entry retires all three at the shared source. Locale Hero/About copy independently promised three live demos. The source tree was independently discoverable through workspace globs and a Dockerfile COPY. Repository-wide caller search found no imports from sibling projects; git history traces the card to `d90933e` and the application to Spec 004. No tests were present in the deleted real-estate source. Historical Spec 004 remains intact and the roadmap section is marked retired.

## Changes

- Removed the real-estate catalogue entry, all three localized project descriptions, and its 36-file independent source tree; removed the root Dockerfile's obsolete COPY.
- Retained sibling projects and tests. Updated README/CLAUDE/hero project counts to three folders. Hero/About now describe two **source-available demo projects**, not an unverified live-service count.
- Pruned retired lockfile importers and their unused graph. Remaining dependency resolutions are unchanged: final lockfile diff contains deletions only. pnpm pruning initially rewrote two unrelated webpack transitive resolutions; their original dependency closure was preserved and frozen installation was rerun successfully.
- Added `scripts/check-retirement.mjs` to `pnpm lint`; Node minimum is 22.6 for native type stripping. Added five isolated mutation checks (`pnpm test:retirement`) and six localized/viewport browser acceptance tests.
- Kept all 22 existing visual cases and their tolerances. Refreshed only the 12 affected home/About baselines in the pinned Noble image, explicitly using `--update-snapshots=all` so below-tolerance copy changes do not retain obsolete pixels.
- Added `scripts/capture-portfolio.mjs` using the existing Playwright dependency; regenerated the existing grid. No video framework/dependency added.
- Fixed the stale Playwright image in the visual README and Biome schema version; ignored only pnpm's generated local dependency cache (`.pnpm-store/`), not source/tests.

## Reproducible verification and actual results

Evidence directory: [`docs/evidence/swarm-2026-09-21`](evidence/swarm-2026-09-21/).

| Command | Actual result / evidence |
|---|---|
| `corepack pnpm install --frozen-lockfile` | PASS, final retained dependency graph; `install-final.log` |
| `corepack pnpm lint` | PASS; 123 files checked, voice 0 hard / 36 existing advisories, retirement guard passes; `lint.log` |
| `corepack pnpm test:retirement` | PASS: clean/restored fixture passes; README link, PT count, EN project reference, sibling deletion and Docker COPY mutations each fail; `retirement.log` |
| `corepack pnpm typecheck` | PASS, 6/6 Turbo tasks; final run 4 cached; `typecheck-final.log` |
| `corepack pnpm build` | PASS, 6/6 tasks; first run uncached, final run 4 cached; `build.log`, `build-final.log` |
| Pinned Noble Playwright, `test --workers=2 --grep '^(.*)(home\|about).*renders' --update-snapshots=all` | PASS 12/12 affected baseline captures; `baselines-all.log` |
| Pinned Noble Playwright, `test --workers=2` | PASS **28/28**, including real-estate HTTP404 and retained projects HTTP200 in EN/PT/ES on desktop/mobile; `visual-final.log` |
| `GET localhost:3000/sitemap.xml` + absence/preservation assertions | PASS, HTTP200, retired slug absent, AI-document/exec-job present; `sitemap.log` |
| `node scripts/capture-portfolio.mjs` in pinned Noble container | PASS; real local production page captures; `media.log` |
| `git diff --check` | PASS |

Use the standalone server procedure in `playwright.config.ts`; Docker browser commands:

```bash
docker run --rm --network=host --ipc=host --user "$(id -u):$(id -g)" \
  -e HOME=/tmp -v "$PWD:/work" -w /work \
  mcr.microsoft.com/playwright:v1.62.1-noble \
  node node_modules/@playwright/test/cli.js test --workers=2
```

Capture reproduction, including the read-only Git metadata mount required by worktrees, is in `tests/visual/README.md`. Disposable browser contexts only; no active browser/account was used. No R2 Nix dependency fetch was running or recorded in its recovered tool calls; unrelated fetches/builds were untouched.

**Limits:** Turbo's Terraform/Python wrapper tasks are existing command echoes, not API or deployment tests. The retained Next.js applications really compiled and typechecked; their Python backends were not exercised. Build warnings include existing Sentry deprecations, exec-job file-tracing warning and no-output wrapper warnings. The visual harness blocks third-party badges; its snapshots are deterministic local rendering evidence, not evidence of external service health. This change does not repair those unrelated services or claims.

## Actual media / provenance

- `docs/assets/portfolio-grid.png`: **1200×900 PNG**, three actual page captures (`/en`, `/en/projects/compliance-tax-agent`, `/en/about`), local production build, no synthetic account/data, no audio, duration not applicable.
- `docs/assets/portfolio-grid.json`: browser/version, capture timestamp, source/message SHA256, PNG SHA256, base and routes. PNG SHA256: `82f2bf3433127f62bebafc2abc08da65f6cfef553d04f31227b7535db19b4932`.
- Renderer: Chromium `151.0.7922.34`; image `mcr.microsoft.com/playwright:v1.62.1-noble`, digest `sha256:dcc5531e97840b9b5e794f2814476b21571c5124a3fca2267d73041f56e7580e`.
- `tests/visual/portfolio.spec.ts-snapshots/{home,home-pt,home-es,about,about-pt,about-es}-{chromium-desktop,mobile}.png`: actual full-page captures. Viewports 1280×800 and 390×844; full-page heights vary. Unrelated project screenshots unchanged.
- Sources: own repository UI and existing fonts/dependencies; no music or third-party footage. Repository MIT license applies to new capture code. Existing dependency/font licenses unchanged.
- The grid and representative home/About mobile captures were opened and inspected. No video slot exists in the current product surface; no video was fabricated or added.

## PR reconciliation and remaining gates

REST facts refreshed 21/09/2026; `pr-facts.log` preserves heads/state:

| PR | Disposition |
|---|---|
| #135, `99483ea782f5fc3c84213d8db6f21829d5e742e6` | OPEN/DRAFT/DIRTY; additionally deletes AI-document functionality. Sonar failed; other checked jobs passed. Replacement scope here is narrower. **Do not close until coordinator disposition.** |
| #136, `7bf01de186c4c78fbefb299351f2ea8401a18481` | OPEN; Next bump only in retired workspace. Lint/typecheck failed, build skipped. Obsoleted by this retirement only after coordinator lands it; not closed. |
| #122, `3c944fd78ecf0f815460dd776560ce6a9cdbe365` | OPEN; checked jobs green. Motion major upgrade remains separate, not cherry-picked. |
| #137, `49bd84465272e513711eb4a16721c8bef3fe0e8e` | OPEN; checked jobs green, Actions changes separately gated. Untouched. |

- Full GLM **exact-head independent review is pending**, owned by coordinator after queue recovery. Local tests are not that verdict.
- Replacement PR CI must pass on its exact head; local green does not certify remote CI.
- Merge/production deployment remains gated: main pushes auto-deploy the portfolio. No merge or deploy attempted.
- One-line rollback **after a future landing**: `git revert <replacement-squash-sha>` on a new feature branch, then submit a revert PR. Nothing has landed in this session.
