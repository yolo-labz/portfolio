# Real-estate retirement

## Scope
Remove the retired real-estate demonstration from the public portfolio and active build graph. Preserve AI-document processing, job aggregation, serverless API, all unrelated routes and tests. Historical specifications remain clearly archival; no live service is changed.

## Acceptance
- No card, localized copy, README or generated route advertises the retired demonstration.
- Retired application dependencies are absent; remaining workspace dependencies stay pinned.
- A runnable regression guard rejects reintroduction and checks preservation of sibling projects.
- Lint, typecheck, build and browser checks execute; screenshots reflect this branch, not a claimed deployment.
- No merge/deploy; independent full GLM review belongs to the coordinator.
